<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Holidays</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <a href="/", class="logo-container">
          <span class="logo"></span>
          <div class="heading-text">Kat and Liv's holiday planning tool</div>
        </a>
        <nav role="navigation">
          <ul class="nav nav-pills">
            <li><%= link "Holiday ideas", to: idea_path(@conn, :index) %></li>
            <li><%= link "Bands", to: band_path(@conn, :index) %></li>
            <li><%= link "Seasons", to: season_path(@conn, :index) %></li>
            <li><%= link "Users", to: user_path(@conn, :index) %></li>
          </ul>
        </nav>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
